spotify.clientId=5a5af3ea4d104213872ebff79136fcda
spotify.clientSecret=${SPOTIFY_CLIENT_SECRET}
spotify.authUri=https://accounts.spotify.com/api/token/
spotify.apiUri=https://api.spotify.com

# Defaults match `docker compose -f backend/docker-compose.yml up -d mysql` (or
# the root compose file) run with no extra setup: MYSQL_ALLOW_EMPTY_PASSWORD=yes,
# root, mapped to localhost:3306.
mysql.host=${MYSQL_HOST:localhost}
mysql.user=${MYSQL_USER:root}
mysql.password=${MYSQL_PASSWORD:}
